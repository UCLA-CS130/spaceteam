# This is a comment.

port 2030;  # This is also a comment.

path /static StaticHandler {
  root ./example_files;
}

path /echo EchoHandler {}

path /status StatusHandler {}

# Default response handler if no handlers match.
default NotFoundHandler {}

path / ProxyHandler{
	host www.ucla.edu;
	portno 80;
}
